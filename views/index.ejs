<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Node App - Clima Tempo ;)</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
        	<label>Bem Vindo!</label>
          	<input name="city" type="text" class="ghost-input" placeholder="Informe a Cidade" required>
          	<input type="submit" class="ghost-button" value="Ver Clima">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
